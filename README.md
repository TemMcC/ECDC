# ECDC

Утилита шифрования Telegram-ID и Telegram-Group-ID

---

## Зачем это нужно

Telegram-ID создаётся один раз и навсегда связан с аккаунтом.
В большинстве стран номер телефона:

* оформляется по паспортным данным

В некоторых странах:

* может быть доступен в утекших и полуофициальных базах
* может быть "пробит" через коммерческие сервисы

Telegram-ID связан с номером телефона, а значит может быть связан с паспортными данными.

Поэтому утечка Telegram-ID может приводить к утечке персональных данных пользователя.

---

## Что делает эта утилита

Утилита решает **прикладную задачу защиты базы данных от утечек**.

Она нужна не для защиты трафика, а для снижения ущерба при компрометации БД.

---

### ✅ Что делает:

* исключает хранение Telegram-ID в базе в открытом виде
* преобразует Telegram-ID в внутренний User-ID
* делает утекшую базу бесполезной без секретных параметров шифрования
* устраняет возможность прямой корреляции между Telegram и базой
* снижает юридические и репутационные риски при утечке

---

### ❌ Что НЕ делает:

* не защищает сервер
* не предотвращает взлом
* не скрывает действия пользователя от Telegram
* не защищает от активных атак

---

## Типовая угроза, от которой защищает утилита

Банальная утечка:

* контейнер с базой размещен в default сети docker
* порт базы проброшен на хост
* порт базы не закрыт firewall
* база собрана с настройками из публичного `docker-compose.yml` или `.env`

результат:

* база скопирована авто-сканерами
* дамп оказался в сети

Без этой утилиты:
→ утекли реальные Telegram-ID
→ утекли персональные данные

С этой утилитой:
→ в БД лежат только зашифрованные идентификаторы
→ восстановление невозможно без ключей

---

## Из чего состоит проект

Проект разделён на три логических уровня:

### 1) `ecdc_core.py`

Криптографическое ядро:

* генерация ключа (PBKDF2)
* формат-сохраняющее шифрование
* работа с числами фиксированной длины

### 2) `ecdc_api.py`

Сервисный слой:

* encapsulation логики в класс `EcdcService`
* подготовка ключа один раз при запуске
* методы:

  * `enc_ttu` / `dec_utt` — пользователи
  * `encg` / `decg` — группы

### 3) `ecdc_cli.py`

CLI-интерфейс:

* ручная проверка работы
* selftest
* отладка и диагностика

---

## Установка

Требуется Python 3.11+

Установить зависимости:

```bash
pip install -r requirements.txt
```

---

## Настройка перед первым запуском

Перед использованием в проекте требуется настроить параметры схемы.

---

### 🚨 ВАЖНО: все параметры схемы являются частью ключа

Изменение ЛЮБОГО из параметров:

* системного `prefix` в `ecdc_core.py:33`
* `ECDC_SECRET`
* `ECDC_TWEAK`
* `ECDC_ITER`
* `ECDC_KDF`

делает невозможной расшифровку ранее сохранённых данных.

---

### 1) ИЗМЕНИТЕ системный `prefix` (строго до эксплуатации)

В файле `ecdc_core.py` есть строка 33:

```python
salt = ("example-prefix-42|" + tweak)
```

Обязательно замените `example-prefix-42` на уникальное новое значение.

---

### 2) ЗАДАЙТЕ переменные окружения

Пример (PowerShell):

```powershell
$env:ECDC_SECRET = "sUpEr_SeCrEt_PaSsWoRd_oHfX0bPKb+jyFCY*LW^9k!HXE7TI]WZFyU="
$env:ECDC_TWEAK  = "example"
$env:ECDC_ITER   = "200000"
$env:ECDC_KDF    = "sha256"
```

---

### Назначение параметров

* `prefix` — фиксированный системный идентификатор схемы

* `ECDC_SECRET` — основной секрет
* `ECDC_TWEAK` — логическое разделение сред (prod / test / demo)
* `ECDC_ITER` — количество итераций PBKDF2
* `ECDC_KDF` — хеш-функция для KDF (`sha256` или `sha1`)

---

## Пример работы CLI

Ниже показан пример работы утилиты с двумя разными конфигурациями ключей и то, что происходит при попытке расшифровки с неправильными параметрами.

Для теста считаем, что в `ecdc_core.py` используется префикс:

```
example-prefix-42
```

---

### Конфигурация №1

```powershell
$env:ECDC_SECRET = "sUpEr_SeCrEt_PaSsWoRd_oHfX0bPKb+jyFCY*LW^9k!HXE7TI]WZFyU="
$env:ECDC_TWEAK  = "example"
$env:ECDC_ITER   = "200000"
$env:ECDC_KDF    = "sha256"
```

```powershell
python ecdc_cli.py enc --tid="123456789"
2235-5049-0840

python ecdc_cli.py dec --uid="2235-5049-0840"
123456789

python ecdc_cli.py encg --tgid="-100123456789"
-7119-7817-4657-9366

python ecdc_cli.py decg --ugid="-7119-7817-4657-9366"
-100123456789
```

---

### Конфигурация №2 (другой `TWEAK`)

```powershell
$env:ECDC_SECRET = "sUpEr_SeCrEt_PaSsWoRd_oHfX0bPKb+jyFCY*LW^9k!HXE7TI]WZFyU="
$env:ECDC_TWEAK  = "test"
$env:ECDC_ITER   = "200000"
$env:ECDC_KDF    = "sha256"
```

```powershell
python ecdc_cli.py enc --tid="123456789"
3730-0940-4205

python ecdc_cli.py dec --uid="3730-0940-4205"
123456789

python ecdc_cli.py encg --tgid="-100123456789"
-1332-0955-3502-5972

python ecdc_cli.py decg --ugid="-1332-0955-3502-5972"
-100123456789
```

---

### Попытка расшифровки с неправильными параметрами

Зашифровано с:

```
ECDC_TWEAK = "example"
```

Пытаемся расшифровать с:

```
ECDC_TWEAK = "test"
```

```powershell
python ecdc_cli.py dec --uid="2235-5049-0840"
504323275003

python ecdc_cli.py decg --ugid="-7119-7817-4657-9366"
-3606279847347417
```

Результат:

* ошибки нет
* данные выглядят валидно
* но это мусор

---

Если:

* `prefix`
* `SECRET`
* `TWEAK`
* `ITER`
* `KDF`

не совпадают с теми, что использовались при шифровании —
расшифровка выдаст случайные данные и не сообщит об ошибке.

Это ожидаемое поведение.

---

# Интеграция в ваш сервис

Утилита рассчитана на работу внутри сервиса/бота:
Telegram-бот, веб-сервис или микросервисная архитектура с авторизацией через Telegram или использующие Telegram-API.
`Все, где Telegram-ID складывается в базу`

Есть два варианта использования:

1. **Глобальный сервис** (один раз инициализируется при старте приложения)
2. **Локальный сервис** (создаёте `EcdcService` вручную там, где нужно)

Оба варианта используют один и тот же API.

---

## 1. Инициализация (глобальный сервис)

Перед использованием необходимо создать подготовленный сервис.
Это делается либо через переменные окружения, либо через ваш класс настроек.

### Вариант A — через переменные окружения

```python
from ecdc_api import init_from_env

svc = init_from_env()
```

Требуется наличие:

```
ECDC_SECRET
ECDC_TWEAK
ECDC_ITER
ECDC_KDF
```

---

### Вариант B — через объект настроек

```python
from pydantic_settings import BaseSettings
from ecdc_api import init_from_settings

class Settings(BaseSettings):
    ECDC_SECRET: str
    ECDC_TWEAK: str = "default"
    ECDC_ITER: int = 200000
    ECDC_KDF: str = "sha256"

settings = Settings()
svc = init_from_settings(settings)
```

---

## 2. Использование глобальных коротких функций

После инициализации можно просто вызывать:

```python
from ecdc_api import enc_ttu, dec_utt, encg, decg

uid = enc_ttu(123456789)
tid = dec_utt(uid)

ugid = encg(-1001234567890)
gid  = decg(ugid)
```

Преимущества:

* нет необходимости передавать объект
* сервис использует кэшированный ключ
* вызовы максимально быстрые

---

## 3. Использование отдельного экземпляра `EcdcService`

Если глобальный объект не подходит:

```python
from ecdc_api import EcdcService

svc = EcdcService(
    secret="your-secret",
    tweak="production",
    kdf="sha256",
    iterations=200000,
)

uid = svc.enc_ttu(42)
tid = svc.dec_utt(uid)

ugid = svc.encg(-1009876543210)
gid  = svc.decg(ugid)
```

---

## 4. Встраивание в Telegram-бота (пример на aiogram)

```python
from aiogram import Bot, Dispatcher
from ecdc_api import init_from_env, enc_ttu

bot = Bot(token=TOKEN)
dp = Dispatcher()

svc = init_from_env()  # инициализация сервиса

@dp.message()
async def handler(msg):
    tid = msg.from_user.id
    uid = enc_ttu(tid)
    await msg.reply(f"Твой UID: {uid}")
```

---

## 5. О производительности

* PBKDF2 вызывается **один раз** при старте
* все операции шифрования/дешифрования:

  * синхронные
  * быстрые
  * работают только на HMAC + арифметике
* overhead минимальный (десятки наносекунд)

---